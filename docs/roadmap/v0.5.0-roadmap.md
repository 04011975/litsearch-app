# LitSearch v0.5.0 Roadmap

## Goal

Prepare the next development cycle after the validated v0.4.0 release.

## Refactoring

- [x] Centralize remaining All Sources helper functions in `app/all_sources.py`
- [ ] Reduce duplicated sorting logic between `app/main.py` and `app/jobs/export_tasks.py`
- [ ] Review large `main.py` sections for possible modularization

## Architecture

- [ ] Add `docs/architecture/decision-log.md`
- [ ] Document major architectural decisions
- [ ] Review logging and remove temporary debug logs where appropriate

## Validation

- [ ] Re-run v0.4.0 validation baseline after helper refactor
- [ ] Confirm All Sources UI/export parity remains intact
- [ ] Confirm source-specific exports remain stable

## Possible Future Features

- [ ] Evaluate additional sources such as Crossref, CORE, or Lens
- [ ] Improve monitoring and observability
- [ ] Add export performance metrics

## Baseline

Starting point:

- Release: `v0.4.0`
- Last validated architecture: All Sources aggregation with UI/export parity

## Performance investigation

- Measure All Sources search latency per source
- Measure deduplication overhead
- Measure sorting overhead
- Measure export generation time
- Compare cold-cache vs warm-cache performance
- Evaluate deque-based source interleaving

### Investigation Scope

This phase focuses on measurement only.

Metrics to collect:

- Total All Sources latency
- PubMed retrieval latency
- OpenAlex retrieval latency
- Europe PMC retrieval latency
- Semantic Scholar retrieval latency
- Deduplication latency
- Sorting latency
- Pagination latency

No performance optimizations will be implemented during this phase.

The goal is to identify actual bottlenecks before making architectural changes.

### All Sources Pipeline Refactor

- Introduced build_all_source_results()
- Introduced fetch_all_source_candidates()
- Removed duplicated retrieval logic from UI and export paths
- Centralized deduplication, sorting and pagination